# Keybindings require three things to be fully defined:
# A selector that is matched against the focused element,
# the keystroke
# and the command to execute.

'atom-workspace':
  'alt-shift-m': 'mitsu:toggle'
